# name: 4h Harmonic Scanner

# on:
#   schedule:
#     # '0 */4 * * *' means "At minute 0 past every 4th hour"
#     # (e.g., 00:00, 04:00, 08:00, 12:00, 16:00, 20:00 UTC)
#     - cron: '0 */4 * * *'
#   workflow_dispatch: # Allows manual trigger

# jobs:
#   scan-and-alert:
#     runs-on: ubuntu-latest
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v3

#     - name: Set up Python
#       uses: actions/setup-python@v4
#       with:
#         python-version: '3.9'

#     - name: Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install -r requirements.txt

#     - name: Run Scanner
#       env:
#         DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL_4H }}
#       run: python 4hrharmonics.py
